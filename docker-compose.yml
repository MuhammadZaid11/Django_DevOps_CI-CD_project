version: "3.9"

services:
  django-app:
    image: muhammadzaid673/django_tweet_app:v2
    container_name: django-app
    restart: always
    ports:
      - "8000:8000"
    environment:
      - DEBUG=False
    command: gunicorn fullstack_project.wsgi:application --bind 0.0.0.0:8000
